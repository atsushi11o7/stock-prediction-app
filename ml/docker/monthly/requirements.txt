# Monthly finetuning dependencies (includes PyTorch CPU)
--extra-index-url https://download.pytorch.org/whl/cpu

# Install numpy first to avoid build issues
numpy>=1.24.0,<2.0.0

# PyTorch CPU version
torch==2.1.2+cpu

# ONNX - use specific versions with pre-built wheels
onnx==1.15.0
onnxruntime==1.16.3

# Data processing
pandas>=2.0.0,<3.0.0
yfinance>=0.2.36

# Utilities
PyYAML>=6.0
boto3>=1.34.0
